#include "PerspectiveCamera.h"

PerspectiveCamera::PerspectiveCamera(Vector3 position, Vector3 lookDirection, float fov, float aspectRatio, float aperture, float focusDistance)
{

}

RTRay PerspectiveCamera::getCamRay(float u, float v) const
{
	return RTRay();
}
